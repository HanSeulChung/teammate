spring:
  config:
    import:
    - classpath:application-db.yml
    - classpath:application-jwt.yml
    - classpath:application-email.yml
    - classpath:application-s3.yml
    - classpath:application-oauth.yml
    - classpath:application-host.yml
    activate:
      on-profile: dev

logging:
  level:
    root: info
